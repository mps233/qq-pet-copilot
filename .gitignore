.venv/
__pycache__/
*.pyc
.env
screenshots
# 打包产物（spec 是 build.py 的输入，需入库，不再忽略）
build/
dist/
# 运行状态（进度、日志、调试图）
runs/
# 控件树抓页产物（tools/dump_hierarchy.py）
xml/
# 个人配置（模板为 config.example.yaml）
config.yaml

# 第三方二进制/离线包（scrcpy、frida-server 等），build 时下载或本地放入，不入库
resources/

# QQ 宠物素材提取产物（约 2.2G、4.3 万个文件）：含 375M 的 base.apk 超出 GitHub 单文件
# 100M 上限；且素材版权归腾讯，不宜入库/发布。素材保留在本地磁盘，不入版本控制。
qqpet_assets/

# macOS 目录元数据
.DS_Store

# 配置热加载信号（运行时生成）
runs/reload.signal
runs/friends_cache.json
